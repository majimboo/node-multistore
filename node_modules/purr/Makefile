MOCHA_OPTS= --check-leaks
REPORTER = dot

check: test

test:
	@NODE_ENV=test ./node_modules/.bin/mocha \
		--reporter $(REPORTER) \
		--recursive \
		--globals setImmediate,clearImmediate \
		$(MOCHA_OPTS)


.PHONY: test
